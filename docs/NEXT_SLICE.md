# Next Implementation Slice

1. Wire discovery jobs into the worker using provider capability checks.
2. Add a permitted manual/fixture seed path for end-to-end Shadow testing while Brave persistence is gated.
3. Add OpenAI Responses API structured extraction behind an interface and evidence-only prompt boundary.
4. Add package/service extraction schemas and Shadow preview endpoints.
5. Add circuit-breaker metrics for crawl/provider/extraction failure rates and spend.
6. Verify PR CI, patch compile/test failures, then merge the foundation before beginning the next feature branch.
