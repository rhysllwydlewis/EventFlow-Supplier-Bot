# Phase 3 handoff

After merge and deployment, start the South Wales Venues campaign only under the Phase 3 Shadow safety contract. The validator will create a fresh run window, count to 100 candidates, drain automatically and expose the final metrics for sign-off.
